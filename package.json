{
  "name": "ai-gateway",
  "version": "0.1.0",
  "description": "Tiny CLI for generating text and images via the Vercel AI Gateway.",
  "type": "module",
  "bin": {
    "ai-gateway": "./bin/ai-gateway.js"
  },
  "files": [
    "bin",
    "src",
    "README.md"
  ],
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "ai-gateway": "bun run src/index.ts"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.0",
    "ai": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
